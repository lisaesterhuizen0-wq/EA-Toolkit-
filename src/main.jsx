import React from 'react';
import ReactDOM from 'react-dom/client';
import EAToolkit from './EA_Toolkit';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EAToolkit />
  </React.StrictMode>
);
